require 'spec_helper'

describe ActiveSniper do
  it 'has a version number' do
    expect(ActiveSniper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
